DATABASE_URL=
# Absolute URL of the ABM analyze workflow endpoint (defaults to the Arena workflow if unset)
ABM_API_URL=
# API key sent upstream as X-API-Key — required, never commit the real value
ABM_API_KEY=
