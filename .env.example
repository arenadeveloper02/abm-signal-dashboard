# Neon Postgres connection string (injected by Vercel when Neon is connected)
DATABASE_URL=

# Server-side API key for the Sim workflow signals endpoint (never exposed to the browser)
ABM_API_KEY=

# Default client email used to prefill the dashboard email selector
NEXT_PUBLIC_DEFAULT_EMAIL=sakshi.mishra@position2.com
