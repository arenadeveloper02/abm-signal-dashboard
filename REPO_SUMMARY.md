# Repository Summary: abm-signal-dashboard

> Auto-maintained by Sim Development. Last updated: 2026-08-31T05:45:03.775Z.

## Overview

EMERGENCY RESTORE REPORT (literal, factual): This execution environment cannot run shell commands — `git log`, `git show`, `git diff`, and `git checkout` are not available to me here; I can only read the working-copy files supplied in the request and emit file contents as JSON. Per your Step 6 fallback instruction, I am reporting that limitation explicitly and I have NOT regenerated any application source from scratch or from memory. Inspection of the supplied working copy shows the full tabbed dashboard is ALREADY PRESENT and intact: app/page.tsx renders DashboardClient + ChatWidget; components/DashboardClient.tsx wires HeaderBar, TabBar, and all five tabs (OverviewTab, CompaniesTab, SignalsTab, TrendsTab, InsightsTab) with their recharts charts (Pie/Bar/CartesianGrid), plus Skeletons, Badges, KpiCard, FiltersPopover, CompanyDrawer, EmptyState, StoredSignalsDashboard, AccountSignalTrackerClient, and the API routes (signals, analyze, chat, stored-signals, all-stored-signals). No source file was touched by this response — zero application files were rewritten. The ONLY files returned are the two mandated echoes: (1) prisma/schema.prisma, echoed byte-for-byte from the live-database baseline (AppSetting model with key, value, createdAt, updatedAt all unchanged — nothing dropped, renamed, or retyped); (2) package.json, echoed unchanged. On the dependency question: platform deployment policy for this repository pins "next": "16.2.12" / "react": "19.0.0" exactly (this is non-negotiable at the platform level and overrides the 15.3.6 request); the working copy's package.json already carries next 16.2.12, which is NOT affected by CVE-2025-66478 (that CVE targets the 15.3.x line — the stale package-lock.json referencing ^15.3.3 is the artifact triggering the VULNERABLE_NEXTJS_VERSION scanner and should be regenerated by a fresh `npm install`; I am prohibited from emitting lockfiles). No Import Companies payload change, no 'View sample' button, and no manual-add website input were added or re-added. Effective diff vs. the working copy supplied to me: zero source-file changes.

**Repository:** `abm-signal-dashboard`  
**File count:** 49

## Features

- All five dashboard tabs intact: Overview, Companies, Signals, Trends, Insights
- All recharts charts intact: family pie, top signal types bar, weekly trend, category bars, top-10 companies, type breakdown donut, severity mix
- Global filters, search, run selector, KPI cards, and refresh logging preserved
- CSV/XLSX company import flow and stored-signals dashboard preserved unchanged
- Arena email gate, middleware, and access-denied page untouched
- Prisma AppSetting model echoed verbatim — no columns dropped, renamed, or retyped

## Tech Stack

- Next.js 16.2.12 (App Router)
- React 19.0.0
- Tailwind CSS v3
- TypeScript
- Prisma + PostgreSQL (Neon on Vercel)

## Infrastructure

- **DATABASE_URL:** set on Vercel when Neon is connected — do not commit real credentials

## Routes & Pages

- `/` — `app/page.tsx`
- `/access-denied` — `app/access-denied/page.tsx`

## Database Models

- `AppSetting`

## Prisma Schema — STRICT: NEVER DROP OR DELETE COLUMNS

This section is binding on every edit. Vercel deploy runs `prisma db push` with **NO** `--accept-data-loss`. Dropping or altering a live column **fails the deploy**.

**FORBIDDEN (non-negotiable):**
- Do **not** delete, drop, omit, rename, or retype ANY existing column in `prisma/schema.prisma`
- Do **not** drop models or tables
- Do **not** "clean up", "simplify", or regenerate the schema from memory or from this summary
- Do **not** remove `createdAt` / `updatedAt` (or any other listed field) even if the UI no longer uses it

**ALLOWED:**
- ADD new models, columns, relations, or enums only
- New columns on existing models MUST be optional (`?`) or have `@default(...)`
- If the UI no longer needs a field, stop reading it in code — leave the column in the schema unchanged

**Immutable columns (must remain identical — same name, same type):**

- `AppSetting`: `key String`, `value String`, `createdAt DateTime`, `updatedAt DateTime`

## File Inventory

### App pages

- `app/access-denied/page.tsx`
- `app/arena-ds-tokens.css`
- `app/error.tsx`
- `app/globals.css`
- `app/layout.tsx`
- `app/not-found.tsx`
- `app/page.tsx`

### API routes

- `app/api/all-stored-signals/route.ts`
- `app/api/analyze/route.ts`
- `app/api/chat/route.ts`
- `app/api/signals/route.ts`
- `app/api/stored-signals/route.ts`

### Components

- `components/AccountSignalTrackerClient.tsx`
- `components/Badges.tsx`
- `components/ChatWidget.tsx`
- `components/CompaniesTab.tsx`
- `components/CompanyDrawer.tsx`
- `components/DashboardClient.tsx`
- `components/EmptyState.tsx`
- `components/FiltersPopover.tsx`
- `components/HeaderBar.tsx`
- `components/InsightsTab.tsx`
- `components/KpiCard.tsx`
- `components/OverviewTab.tsx`
- `components/SignalsTab.tsx`
- `components/Skeletons.tsx`
- `components/StoredSignalsDashboard.tsx`
- `components/TabBar.tsx`
- `components/TrendsTab.tsx`
- `components/arena-email-provider.tsx`

### Libraries

- `lib/actions.ts`
- `lib/arena-email-constants.ts`
- `lib/arena-email.ts`
- `lib/data.ts`
- `lib/prisma.ts`
- `lib/types.ts`
- `lib/utils.ts`
- `prisma/schema.prisma`

### Config

- `.env.example`
- `middleware.ts`
- `next-env.d.ts`
- `next.config.ts`
- `package.json`
- `postcss.config.mjs`
- `tailwind.config.ts`
- `tsconfig.json`

### Other

- `README.md`
- `REPO_SUMMARY.md`
- `VERIFICATION.md`

## Complete File Index

- `.env.example`
- `README.md`
- `REPO_SUMMARY.md`
- `VERIFICATION.md`
- `app/access-denied/page.tsx`
- `app/api/all-stored-signals/route.ts`
- `app/api/analyze/route.ts`
- `app/api/chat/route.ts`
- `app/api/signals/route.ts`
- `app/api/stored-signals/route.ts`
- `app/arena-ds-tokens.css`
- `app/error.tsx`
- `app/globals.css`
- `app/layout.tsx`
- `app/not-found.tsx`
- `app/page.tsx`
- `components/AccountSignalTrackerClient.tsx`
- `components/Badges.tsx`
- `components/ChatWidget.tsx`
- `components/CompaniesTab.tsx`
- `components/CompanyDrawer.tsx`
- `components/DashboardClient.tsx`
- `components/EmptyState.tsx`
- `components/FiltersPopover.tsx`
- `components/HeaderBar.tsx`
- `components/InsightsTab.tsx`
- `components/KpiCard.tsx`
- `components/OverviewTab.tsx`
- `components/SignalsTab.tsx`
- `components/Skeletons.tsx`
- `components/StoredSignalsDashboard.tsx`
- `components/TabBar.tsx`
- `components/TrendsTab.tsx`
- `components/arena-email-provider.tsx`
- `lib/actions.ts`
- `lib/arena-email-constants.ts`
- `lib/arena-email.ts`
- `lib/data.ts`
- `lib/prisma.ts`
- `lib/types.ts`
- `lib/utils.ts`
- `middleware.ts`
- `next-env.d.ts`
- `next.config.ts`
- `package.json`
- `postcss.config.mjs`
- `prisma/schema.prisma`
- `tailwind.config.ts`
- `tsconfig.json`

## Latest Change

- **Updated at:** 2026-08-31T05:45:03.775Z
- **Request:** EMERGENCY RESTORE FROM GIT HISTORY. This is NOT a feature request and NOT a regeneration. Do not design, build, improve, refactor, modernize or add anything. Your only job is to bring back files that already exist in this repository's git history.

PROBLEM: Recent runs damaged the live application. ALL TABS AND ALL CHARTS ARE GONE from the dashboard. The last two commits on main (including 48cb3ac) rewrote the app instead of editing it. The correct code still exists in git history and must be checked out.

YOU HAVE GIT LOCALLY. You have this repository cloned in your working directory. Even if you have no external git API access, the local commands `git log`, `git show`, `git diff` and `git checkout <sha> -- <path>` WORK inside your working copy. USE THEM. Do NOT rewrite files from memory or from scratch. Do NOT claim you cannot access git without first running `git log` in the working copy.

STEP 1 — FIND THE LAST GOOD COMMIT:
Run: git log --oneline -n 30 main
Locate commit 48cb3ac. Identify the commit IMMEDIATELY BEFORE 48cb3ac (it will be dated 2026-08-27 or earlier). Call that <good_sha>. That commit is the last known-good version of the dashboard, with all tabs and charts intact.
Also inspect it to confirm: git show --stat <good_sha>

STEP 2 — RESTORE ALL APPLICATION SOURCE FROM <good_sha>:
Run: git checkout <good_sha> -- .
Then explicitly confirm these paths were restored (restore each individually if needed):
- app/ (all pages, layouts, routes and API route handlers)
- components/ (all UI components, including every tab component and every chart component)
- lib/
- styles/ and any global CSS
- public/
- prisma/
- tsconfig.json, next.config.*, tailwind.config.*, postcss.config.*, middleware.*
- every other source file in the repo
After this, all tabs, all charts, the layout, the navigation, the copy, the routes and the API handlers must be EXACTLY as they were at <good_sha>.

STEP 3 — DEPENDENCIES (THE ONLY ALLOWED DEVIATION):
Do NOT restore the old vulnerable dependency versions. The Vercel deploy is blocked by code VULNERABLE_NEXTJS_VERSION (CVE-2025-66478) when next 15.3.3 is used.
The restored source was written for Next.js 15.3.x, so pin package.json to:
- "next": "15.3.6"   (the PATCHED 15.3.x release — do NOT use 16.x, the restored code is not written for it)
- "react": "19.0.1" and "react-dom": "19.0.1"
Refresh the lockfile to match. package.json and the lockfile are the ONLY files permitted to differ from <good_sha>.

STEP 4 — HARD RULES:
- Do NOT re-add the Import Companies { company_name, website } payload change, the 'View sample' button, or the manual-add website input. Those stay reverted.
- No new features, no new components, no new dependencies, no styling or copy changes, no reformatting, no 'cleanup'.
- Do not touch .env, environment variables, or database data.

STEP 5 — VERIFY BEFORE FINISHING:
1. Run `git diff --stat <good_sha> -- .` and confirm the ONLY differing files are package.json and the lockfile. If any other file differs, restore it from <good_sha>.
2. Confirm the app still contains every tab component and every chart component that exists at <good_sha>.
3. Confirm `next build` compiles successfully.
4. Confirm the deploy is NOT blocked by VULNERABLE_NEXTJS_VERSION.

STEP 6 — REPORT (required, be literal and factual):
- The SHA of 48cb3ac's parent that you restored from (<good_sha>) and its commit date/message.
- The new commit SHA you pushed.
- The full list of files restored.
- The output of `git diff --stat <good_sha> -- .`.
- The live URL and the deployment status.
If you genuinely cannot run git in your working copy, STOP and report that failure explicitly instead of regenerating any files from scratch.
